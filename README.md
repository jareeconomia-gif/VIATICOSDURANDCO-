# Portal de Viáticos Durandco

Paquete corregido para GitHub + Render SIN carpeta `public`.

Sube directamente a la raíz del repositorio:
- Dockerfile
- index.html
- package.json
- server.js
- render.yaml

Después en Render:
Manual Deploy → Deploy latest commit

Health check:
`/api/health`
